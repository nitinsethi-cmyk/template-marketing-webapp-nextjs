/**
 * Amplitude Analytics API key — must match the key passed to
 * `window.amplitude.init(...)` in `_document.tsx` so SSR can read/write the
 * same Browser SDK 2.0 identity cookie.
 */
export const AMPLITUDE_API_KEY = '13763c810e8e420a211a47efe6d52a1a';
